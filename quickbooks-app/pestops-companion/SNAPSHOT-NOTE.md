# Temporary snapshot

This is a snapshot of the `pestops-companion` project, parked here because the
standalone private repo could not be created from the remote session (the
GitHub integration lacks repo-creation permission).

Once the private `pestops-companion` repo exists on GitHub, move this code
there (push this tree as the initial commit) and delete this directory from
learn-git.
